# Deployment

Deployment details are intentionally deferred until the beta-first runtime layout is established.

For now:

- keep generated binaries out of git
- validate assets and tests locally before syncing to a game install
- treat Steam and local mod deployment paths as machine-local configuration
